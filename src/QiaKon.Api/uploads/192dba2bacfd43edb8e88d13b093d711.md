# MiniMax 流程联调文档

QiaKon 现在应该在文档上传后触发真实的分块和索引流程。

## 关键事实

- 文档上传完成后，系统会解析文档内容。
- 如果选择 Auto，且存在默认的分块模型，系统会优先使用 MoE 分块。
- 检索阶段会把分块写入 PostgreSQL 向量库。
- 对话阶段会先做向量召回，再调用默认推理模型生成答案。

## 验证问题

用户问：上传之后如何触发 MoE 分块？
答案应当提到：默认分块模型、Auto 策略、以及重新解析会重走索引。
